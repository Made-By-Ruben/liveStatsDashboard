// import type { Action } from 'svelte/action';

// export const countUp: Action = (node, data: { target: number }) => {
// 	// the node has been mounted in the DOM

// 	$inspect(node);

// 	$effect(() => {
// 		// setup goes here

// 		return () => {
// 			// teardown goes here
// 		};
// 	});
// };
